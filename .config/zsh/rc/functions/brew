#!/usr/bin/env zsh

# Homebrew wrapper - Auto-refresh completions after install/uninstall
command brew "$@"
if [[ "$1" == "install" || "$1" == "uninstall" ]]; then
  echo "Refreshing completions..."
  rm -f ${ZDOTDIR:-~}/.zcompdump*
  compinit
fi
